<Window x:Class="AsyncAwait_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AsyncAwait_App"
        mc:Ignorable="d"
        Title="AsyncAwait App v1.0" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Content="AsyncAwait App v1.0" FontSize="35" Margin="10"/>

        <Button Grid.Row="1" Content="SyncLoad" FontSize="20"
                Margin="10" Padding="10" Click="BtnSync_Click"/>

        <Button Grid.Row="2" Content="AsyncLoad" FontSize="20"
                Margin="10" Padding="10" Click="BtnAsync_Click"/>

        <TextBlock Name="textBlock" Grid.Row="3" Margin="10"/>
    </Grid>
</Window>
